# Unpinned inputs -> install resolves to the latest compatible release.
# After a successful install, run src/common/env_snapshot.py to produce
# requirements.lock.txt, which IS pinned and IS committed.

# --- core ---
numpy
pandas
scipy
scikit-learn
pyyaml
openpyxl          # xlsx reading (S1)

# --- embeddings / models ---
torch
transformers
sentence-transformers
accelerate

# # --- clustering / stats (Phase 2) ---
# hdbscan
# umap-learn        # VISUALIZATION ONLY - never cluster in UMAP space
# statsmodels

# --- agents / RAG (Phase 4) --- ENABLED 2026-08-11
langgraph
chromadb
groq                  # generator, primary (pipeline SS4.4)
google-generativeai   # generator, secondary on a subset (pipeline SS4.4)
fastapi               # demo API; server-side key, RAG and verifier orchestration
uvicorn               # ASGI server for the demo API

# # --- eval (Phase 5) ---
# mauve-text
# netcal
# nltk

# # --- plotting / tracking ---
# matplotlib
# seaborn
# tqdm
# wandb
